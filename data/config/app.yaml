app:
  app_name: aliya-cosmos
  env: dev
  debug: true

log:
  level: INFO
  json: false
  dir: logs
  rotation: "00:00"
  retention: 7 days
  compression: zip

service:
  start_timeout: 30
  stop_timeout: 30

clock:
  tz: UTC

# 密钥类字段以占位符声明，由 .env 提供值：
# api_key: ${DEEPSEEK_API_KEY}
